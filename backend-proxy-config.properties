# Agregar estas propiedades al archivo application.properties del backend principal

# ============================================================================
# 🔗 CONFIGURACIÓN DE PROXY A BACKEND DE HORARIOS
# ============================================================================

# URL del backend de horarios (interno)
app.horarios.api.url=http://localhost:8082
app.horarios.api.timeout=10000

# Configuración de RestTemplate/WebClient
app.horarios.api.connection-timeout=5000
app.horarios.api.read-timeout=10000
